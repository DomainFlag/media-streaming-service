import React from "react"
import {Component} from "react"

import "./style.sass"

export default class AmusingAuth extends Component {
    render = () => (
        <div className="amusing-auth">
            <p className="amusing-auth-header">Cool & interactive snippet that plays instruments whenever the user is filling authentication form.</p>
        </div>
    );
}
