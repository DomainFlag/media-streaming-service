import React from "react"
import {Component} from "react"
import {connect} from "react-redux"

import logo from "../../../resources/assets/logo.svg"
import {ACTIONS} from "../../../reducers/main"

import "./style.sass"

export class FirePlace extends Component {
    constructor(props) {
        super(props);
    }

    render = () => (
        <div className="gig">
        </div>
    )
}